
<div class="video-details">
  <% if !video.live %>
    Duración: <%= Time.at(video.duration||0).gmtime.strftime('%R:%S') + "s" %> | Emitido: <%= video.created_at %>
  <% end %>
</div>
<div class="video-details">
  <%= video.views %> reproducciones
  <%= video.live ? "(" + video.viewers.to_s + " ahora)" : " (" + video.viewers.to_s + " en directo)" %>
  <%= " | Max.: " + video.max_viewers.to_s if video.live || current_channel == video.channel %>
</div>

